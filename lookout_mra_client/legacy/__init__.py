"""
Legacy MRA v1 modules — not used by the v2 connector (main.py).

These are kept for reference and for deployments that use the DB-based
MRAEventRunnerV2 entry point.  New code should use mra_v2_stream_thread
and the event_forwarders package directly.
"""
